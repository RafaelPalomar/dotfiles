.aider*

# Generated documentation (auto-generated from RST sources).
# Anchored to repo root so per-scope CLAUDE.md files (e.g. user-level
# dotfiles/.claude/CLAUDE.md) are still tracked.
/CLAUDE.md

# Sphinx build artifacts
docs/build/

# sops: never commit plaintext secrets.
# sops/lovelace.yaml should only be committed AFTER encrypting with:
#   sops --encrypt --in-place sops/lovelace.yaml
# The .sops.yaml creation rules enforce encryption on write.
