# NNNN. &lt;Short imperative title&gt;

- **Status:** Proposed | Accepted | Superseded by NNNN | Deprecated
- **Date:** YYYY-MM-DD
- **Deciders:** Rafael Palomar
- **PR / commit:** *(filled in on merge)*

## Context

What is the problem? What forces are in play? What constraints (key
material, threat model, hardware, deploy mechanics, server config,
operator capacity) bound the solution space? Cite specifics — a
fingerprint, a config line, a server's `MaxAuthTries`, an incident.

## Decision

What did we decide? Be precise — name the file, key, service, or config
field affected. State it as a present-tense imperative.

## Alternatives considered

For each alternative seriously considered, one sub-section naming what
it would have looked like and the specific failure mode that rejected
it.

## Consequences

What becomes easier? What becomes harder? What follow-up work does this
imply (other ADRs, tooling, runbook entries, inventory rows)?

## Conformance

The invariant this ADR encodes, pinned to something concrete (a `keys
audit` check, a test, an inventory row).

## References

Related ADRs, PKS notes, upstream issues, inline `.scm`/script headers
that codify the same invariant.
