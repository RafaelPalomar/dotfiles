# Sphinx + ReadTheDocs build dependencies for the entelequia docs.
#
# Mirrors the ~/src/Slicer-Liver `requirements-docs.txt` (its ADR-0017)
# to keep the docs toolchain consistent across the author's repos.
#
# Local build (Guix host): the same packages are available via
#   guix shell python-sphinx python-sphinx-rtd-theme python-myst-parser \
#     python-sphinxcontrib-mermaid -- make -C docs html
# RTD uses this file directly.
docutils
linkify-it-py
lxml>=4.9.1
myst-parser
pygments>=2.15.1
# Workaround for readthedocs bug (https://github.com/readthedocs/sphinx_rtd_theme/issues/1343)
sphinx>=5.0,!=5.2.0.post0
sphinx-jsonschema
sphinx_markdown_tables>=0.0.17
sphinx-notfound-page
sphinx-design
sphinx-reredirects
sphinx_rtd_theme>=0.5.2
# Mermaid for the secrets decision-tree flowchart in secrets.rst and
# any architecture diagrams added later.
sphinxcontrib-mermaid
